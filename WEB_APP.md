# Insta Library 本地 Web App

这是一个完全运行在 Mac 上的只读 Web App。网页只连接本机 API；本机 API 负责保持相机的 `6666/TCP` 控制会话，并代理 SD 卡文件的 HTTP Range 请求。

## 使用方法

1. 打开相机 Wi‑Fi，并保持相机唤醒。
2. 完全退出 iPhone 上的 Insta360 App。
3. 让 Mac 连接相机 Wi‑Fi。此时没有互联网是正常的。
4. 在 Finder 中双击项目目录里的 `Insta Library.app`，浏览器会自动打开。

不再需要手动打开终端。命令行启动方式仅作为备用：

```bash
cd /Users/molier/Desktop/Myself/insta360
.venv/bin/python tools/run_web_app.py
```

首次启动本地服务可能需要约半分钟。准备完成后浏览器会自动打开：

```text
http://localhost:3000/
```

点击“连接相机”，应用会读取素材目录。之后可以搜索、筛选、预览 JPG/视频或下载原文件。素材默认按自然日分组，每页最多显示 300 项。点击“批量选择”后，既能选择本页、跨页选择，也能一次选择某一天的全部素材并下载。图片和视频加载期间会显示即时骨架占位；只有接近视口的素材才启动网络加载和骨架动画，预览失败时保留文件类型占位。

使用结束后，点击网页上的“断开并退出”。应用会先安全断开相机，再关闭网页和本地 API 服务，不需要按 `Ctrl+C`。如果重复双击图标，应用会直接打开已经运行的页面，不会重复启动服务。

## 离线能力

依赖已经安装在项目中。后续连接相机 Wi‑Fi 时不需要访问互联网，网页、API、预览和下载都在本地完成。

## 安全边界

- 控制协议只允许命令 `8`（设备/存储信息）和 `13`（文件列表）。
- 不包含拍摄、设置、删除、格式化或时间同步命令。
- 文件代理只能访问本次目录响应中出现的精确路径。
- API 只监听 `127.0.0.1`，局域网内其他设备无法访问。

## 当前兼容范围

- 应用会优先读取相机的 HTTP 目录索引，这条路径没有 500 个文件的上限。
- 如果相机固件关闭了 HTTP 目录索引，会自动降级到已经验证过的只读 UCD2 固定分页帧；这种情况下最多能确认前 500 个文件，网页会明确显示黄色提示，不会把截断结果伪装成完整列表。
- JPG 可直接预览；视频卡片会优先使用同名 LRV 低码率文件生成缩略图，没有 LRV 时从原视频取首帧；MP4/LRV 支持浏览器 Range 播放；DNG 提供原文件下载。
- 网页分页是每页 300 项，页数按实际读取到的文件总数动态计算。
